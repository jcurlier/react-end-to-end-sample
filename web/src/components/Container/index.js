import { Container as SemanticContainer } from 'semantic-ui-react';
import system from 'system-components';

const Container = system({
  is: SemanticContainer,
  py: 4,
});

export default Container;
